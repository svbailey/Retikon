# retikon_core/data_factory/model_registry.py

Edition: Core

## Functions
- `model_registry_uri`: Function that builds the model registry URI, so the system works as expected.
- `load_models`: Function that loads models, so the system works as expected.
- `save_models`: Function that saves models, so the system works as expected.
- `register_model`: Function that registers model, so the system works as expected.
- `update_model`: Function that updates model, so the system works as expected.
- `_normalize_list`: Internal helper that cleans up list, so the system works as expected.
- `_model_from_dict`: Internal helper that builds model from a dict, so the system works as expected.
- `_coerce_optional_str`: Internal helper that converts optional str, so the system works as expected.
- `_coerce_iterable`: Internal helper that converts iterable, so the system works as expected.
- `_coerce_metrics`: Internal helper that converts metrics, so the system works as expected.

## Classes
- `ModelRecord`: Data structure or helper class for Model Record, so the system works as expected.
