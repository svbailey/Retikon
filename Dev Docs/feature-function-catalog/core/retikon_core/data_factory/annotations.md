# retikon_core/data_factory/annotations.py

Edition: Core

## Functions
- `_dataset_uri`: Internal helper that builds the dataset URI, so the system works as expected.
- `_annotation_uri`: Internal helper that builds the annotation URI, so the system works as expected.
- `create_dataset`: Function that creates dataset, so the system works as expected.
- `add_annotation`: Function that add annotation, so the system works as expected.
- `list_datasets`: Function that lists datasets, so the system works as expected.
- `list_annotations`: Function that lists annotations, so the system works as expected.
- `_read_records`: Internal helper that reads records, so the system works as expected.
- `_join_tags`: Internal helper that join tags, so the system works as expected.

## Classes
- `DatasetRecord`: Data structure or helper class for Dataset Record, so the system works as expected.
- `AnnotationRecord`: Data structure or helper class for Annotation Record, so the system works as expected.
