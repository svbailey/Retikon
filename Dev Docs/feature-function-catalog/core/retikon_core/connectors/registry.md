# retikon_core/connectors/registry.py

Edition: Core

## Functions
- `load_registry`: Function that loads registry, so connectors can send and receive data.
- `list_connectors`: Function that lists connectors, so connectors can send and receive data.
- `_normalize_list`: Internal helper that cleans up list, so connectors can send and receive data.
- `_connector_from_dict`: Internal helper that builds connector from a dict, so connectors can send and receive data.

## Classes
- `ConnectorSpec`: Data structure or helper class for Connector Spec, so connectors can send and receive data.
