# retikon_core/services/query_config.py

Edition: Core

## Classes
- `QueryServiceConfig`: Data structure for query service settings, so local and Pro query services stay consistent.
  - Methods
    - `from_env`: Builds settings from env vars, so configuration is centralized.
