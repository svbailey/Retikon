# V3.2 Sprint 00 - v3.1 Carryover Closeout (BYOC + GPU)

Dates: TBD
Owner: Platform Eng (Infra) + DevOps

## Goal
Close v3.1 carryover items so v3.1 can be marked production-ready.

## Scope checklist (Pro)
- [ ] BYOC adapter maturity (K8s): implement real providers in `k8s_adapter/`
      (object store, queue, secrets, state).
- [ ] Add smoke tests: `tests/pro/test_k8s_adapter_smoke.py`.
- [ ] Validate BYOC deployment path in staging.
- [ ] Deploy GPU services via `infrastructure/terraform/`.
- [ ] Enable GPU tier in environment (`query_gpu_enabled=true`).
- [ ] Confirm CPU + GPU tiers available in Pro.

## Primary paths (Pro)
- `k8s_adapter/`
- `retikon_core/providers/`
- `infrastructure/terraform/`

## Tests and validation
- [ ] Pro: `tests/pro/test_k8s_adapter_smoke.py` (new)
- [ ] Pro: `tests/pro/test_query_tiers.py` (GPU tier smoke)

## Deliverables
- [ ] BYOC deployment path validated in staging.
- [ ] CPU + GPU query tiers available in Pro.

## Risks / Dependencies
- [ ] Requires GPU quota and a GPU-capable Cloud Run region.

## Notes
-
